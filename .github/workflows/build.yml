name: C++ Build and Artifact Export

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  build:
    runs-on: windows-latest


    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4   # ✅ 同步升级到 v4

    - name: ⚙️ Install GCC
      run: |
        sudo apt update
        sudo apt install -y g++

    - name: 🏗️ Build C++ Project
      run: |
        echo "正在编译程序..."
        g++ -o crash_simulator main.cpp -std=c++11
        echo "✅ 编译成功，开始执行..."

    - name: 🚀 Run program safely
      run: |
        echo "运行模拟程序（会触发崩溃测试）"
        ./crash_simulator || true
        echo "✅ 运行结束"

    # ✅ 已更新：使用最新 actions/upload-artifact@v4
    - name: 📦 Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: crash_simulator_build
        path: |
          crash_simulator
          main.cpp
          README.md
          CMakeLists.txt
